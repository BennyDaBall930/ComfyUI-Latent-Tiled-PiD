[project]
name = "comfyui-latent-tiled-pid"
description = "Tile the latent, not the pixels: latent-native tiled decoding for NVIDIA PiD. Seam-free 33MP+ masters from 2MP latents; avoids the past-envelope color collapse of single-shot decoding. No VAE re-encode round-trip."
version = "1.0.0"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/BennyDaBall930/ComfyUI-Latent-Tiled-PiD"

[tool.comfy]
PublisherId = "bennydaball930"
DisplayName = "Latent-Tiled PiD"
Icon = ""
