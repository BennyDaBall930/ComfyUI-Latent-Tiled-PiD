[project]
name = "comfyui-latent-tiled-pid"
description = "Tile the latent, not the pixels: latent-native tiled decoding for NVIDIA PiD. Seam-free masters up to 244MP; size-picker dropdowns with render-tested presets for Krea2/qwen AND FLUX.2 Klein 4B/9B; avoids the past-envelope color collapse of single-shot decoding. No VAE re-encode round-trip."
version = "1.3.0"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/BennyDaBall930/ComfyUI-Latent-Tiled-PiD"

[tool.comfy]
PublisherId = "bennydaball"
DisplayName = "Latent-Tiled PiD"
Icon = ""
